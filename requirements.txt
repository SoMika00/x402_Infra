# Web + API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.7.0

# DB
SQLAlchemy>=2.0.30
asyncpg>=0.29.0
aiosqlite>=0.19.0

# Metrics
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=6.1.0

# AI
sentence-transformers>=2.6.1
# Torch CUDA 12.1 : installé via --extra-index-url déjà dans ton Dockerfile
torch==2.4.0+cu121 ; platform_system == "Linux"
# (optionnel) torchvision si besoin
# torchvision==0.19.0+cu121 ; platform_system == "Linux"

numpy>=1.26.0

# GPU telemetry
nvidia-ml-py3>=7.352.0

# Utils
typer>=0.12.3
pyjwt>=2.8.0
cryptography>=42.0.0

# Tests
pytest>=8.2.0

python-multipart>=0.0.9
